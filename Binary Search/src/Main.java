public class Main {
    public static void main(String[] args) {
        System.out.println("Binary search");
/*
        Algo of Binary Search

        1. Find the middle element.
        2. target>mid => search in the right else search in the let
        3. If middle element == target element # answer
        Why Binary Search?
        N/(2^0) , N/(2^1), N/(2^2) ............... N/(2^k),
        MAX time loop will run - 1 = N/(2^k)
        N = 2^k
        log(N) = log(2^k)
        log(N) = k * log(2)
        k = log(N) / log(2)
        k = log(base 2) N     # total comparison in worst case

        complexity is O(log N)          # big O(log N)

*/


    }
}